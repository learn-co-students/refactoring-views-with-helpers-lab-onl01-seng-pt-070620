
<ul>
<% @songs.each do |song| %>
  <li><%= link_to song.title, song_path(song) %></li> - <%= display_artist song %>
  <% if song.artist_name.blank? %>
    <%= link_to "Add Artist", edit_song_path(song) %><br>
    <% end %>
<% end %> 

</ul>
<br>
<%= link_to "Add New Song", new_song_path %><br>

